# local quarantine for files pending manual deletion
.attic/

# JS tooling
node_modules/

# secrets and local state
.env
.dev.vars
*.log
.claude/settings.local.json

# per-session sandbox outputs when testing spawn.sh locally
outputs/

# docs site build artifacts
site/dist/
site/.astro/
