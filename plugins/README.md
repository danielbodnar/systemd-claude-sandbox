# Marketplace plugins

Plugin directories referenced by .claude-plugin/marketplace.json (metadata.pluginRoot) live here. The marketplace currently ships no plugins; add a directory with a .claude-plugin/plugin.json and list it in the manifest.
